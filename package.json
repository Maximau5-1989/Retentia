{
  "name": "retentia",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build --mode chrome --configLoader runner && vite build --mode firefox --configLoader runner",
    "build:chrome": "tsc --noEmit && vite build --mode chrome --configLoader runner",
    "build:firefox": "tsc --noEmit && vite build --mode firefox --configLoader runner",
    "build:all": "tsc --noEmit && vite build --mode chrome --configLoader runner && vite build --mode firefox --configLoader runner",
    "test": "vitest run --configLoader runner",
    "check": "vitest run --configLoader runner && tsc --noEmit && vite build --mode chrome --configLoader runner && vite build --mode firefox --configLoader runner",
    "data:categories": "node scripts/build-category-database.mjs",
    "release:chrome:patch": "node scripts/release.mjs chrome patch",
    "release:chrome:minor": "node scripts/release.mjs chrome minor",
    "release:chrome:major": "node scripts/release.mjs chrome major",
    "release:chrome:dry-run": "node scripts/release.mjs chrome patch --dry-run",
    "release:firefox:patch": "node scripts/release.mjs firefox patch",
    "release:firefox:minor": "node scripts/release.mjs firefox minor",
    "release:firefox:major": "node scripts/release.mjs firefox major",
    "release:firefox:dry-run": "node scripts/release.mjs firefox patch --dry-run"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/chrome": "^0.2.5",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
